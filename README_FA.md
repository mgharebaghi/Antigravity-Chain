# زنجیره آنتی‌گراویتی (Antigravity Chain) 🚀
**سریع‌ترین موتور غیرمتمرکز لایه ۱ جهان**

آنتی‌گراویتی یک بلاک‌چین انقلابی لایه ۱ است که با استفاده از **Tauri**، **Rust** و **React** ساخته شده است. این شبکه از معماری منحصر به فرد **Parallel DAG** و اجماع مبتنی بر **VDF** (اثبات صبر) برای دستیابی به توان عملیاتی فوق‌العاده بالا بدون به خطر انداختن تمرکززدایی استفاده می‌کند.

## 🌟 ویژگی‌های کلیدی
- **اجرای موازی (Parallel Execution)**: پردازش تراکنش‌ها با تحلیل وابستگی‌ها با استفاده از منطق Block-STM.
- **سرعت فوق‌العاده (TPS بالا)**: طراحی شده برای عبور از ۱۵۰,۰۰۰ تراکنش در ثانیه روی سخت‌افزارهای خانگی.
- **سقف عرضه ۲۱ میلیونی**: توکنومیکس دقیق ضد تورمی الهام گرفته از استاندارد بیت‌کوین.
- **اثبات صبر (PoP)**: اجماع سازگار با محیط زیست و مقاوم در برابر حملات سیبیل با استفاده از توابع تأخیر قابل تأیید.
- **شبکه Mesh P2P**: ارتباطات نظیر به نظیر قدرتمند با استفاده از `libp2p`.

## 🛠 تکنولوژی‌های مورد استفاده
- **هسته اصلی**: Rust (Tauri)
- **فرانت‌اند**: React + TypeScript + Tailwind CSS
- **شبکه**: libp2p (Gossipsub, Kademlia DHT)
- **پایگاه داده**: ReDB (ذخیره‌سازی محلی با کارایی بالا)
- **رمزنگاری**: Ed25519, SHA-256, VDF (Patience Proof)

## 🚀 شروع به کار

### پیش‌نیازها
- [Node.js](https://nodejs.org/) (نسخه ۱۸ به بالا)
- [Rust](https://rustup.rs/) (نسخه Stable)
- [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) (برای کاربران ویندوز)

### نصب
۱. مخزن را کلون کنید.
۲. وابستگی‌ها را نصب کنید:
   ```bash
   npm install
   ```

### اجرا در حالت توسعه
```bash
npm run tauri dev
```

## 📖 مستندات
- [وایت‌پیپر فنی (انگلیسی)](WHITEPAPER.md)
- [وایت‌پیپر فنی (فارسی)](WHITEPAPER_FA.md)
- [راهنمای توکنومیکس](documentation.md)

## ⚖️ خلاصه توکنومیکس
- **کل عرضه**: ۲۱,۰۰۰,۰۰۰ AGT
- **تخصیص جنسیس**: ۵,۰۰۰,۰۰۰ AGT
- **پاداش اولیه**: ۴۰ AGT در هر بلاک
- **مکانیزم**: هاوینگ اسپرینت (مدل متمرکز بر نایابی سریع)

---
*ساخته شده با ❤️ برای آینده سیستم‌های مالی غیرمتمرکز.*
